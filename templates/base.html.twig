<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Welcome!{% endblock %}</title>
    <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">
    {% block stylesheets %}
        {{ encore_entry_link_tags('app') }}
    {% endblock %}
    {% block javascripts %}
        {{ encore_entry_script_tags('app') }}
        {% block importmap %}{{ importmap('app') }}{% endblock %}
    {% endblock %}
</head>
<body class="bg-gray-50 dark:bg-gray-900 text-gray-800 dark:text-gray-100">
    <header>
        <nav class="bg-white border-gray-200 dark:bg-gray-900 shadow-md">
            <div class="max-w-screen-xl mx-auto p-4 flex flex-wrap items-center justify-between">
                <a href="{{ path('app_main') }}" class="flex items-center space-x-3">
                    <img src="{{ asset('images/logo.webp') }}" class="h-12 sm:h-16 w-auto" alt="Mypull Logo">
                    <span class="text-lg sm:text-2xl font-semibold text-red-900 dark:text-white">MYPULL</span>
                </a>
                <button data-collapse-toggle="navbar-default" type="button" class="inline-flex items-center p-2 w-10 h-10 text-gray-500 rounded-lg md:hidden hover:bg-gray-100 dark:text-gray-400 dark:hover:bg-gray-700">
                    <span class="sr-only">Open main menu</span>
                    <svg class="w-5 h-5" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 17 14">
                        <path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M1 1h15M1 7h15M1 13h15"/>
                    </svg>
                </button>
                <div class="hidden w-full md:block md:w-auto" id="navbar-default">
                    <ul class="flex flex-col md:flex-row md:space-x-6 items-center mt-4 md:mt-0">
                        <li>
                            <a href="{{ path('app_main') }}" class="block py-2 px-4 text-lg text-red-900 hover:text-white hover:bg-blue-700 rounded">Accueil</a>
                        </li>
                        <li>
                            <a href="{{ path('app_panier') }}" class="block py-2 px-4 text-lg text-red-900 hover:text-white hover:bg-blue-700 rounded">
                                <svg class="w-6 h-6 inline-block text-gray-800 dark:text-white" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24">
                                    <path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 4h1.5L9 16m0 0h8m-8 0a2 2 0 1 0 0 4 2 2 0 0 0 0-4Zm8 0a2 2 0 1 0 0 4 2 2 0 0 0 0-4Zm-8.5-3h9.25L19 7H7.312"/>
                                </svg>
                                Panier
                            </a>
                        </li>
                        {% if is_granted('ROLE_ADMIN') %}
                        <li>
                            <a href="{{ path('app_productadd') }}" class="block py-2 px-4 text-lg text-red-900 hover:text-white hover:bg-blue-700 rounded">Dashboard</a>
                        </li>
                        {% endif %}
                        {% if app.user %}
                        <li>
                            <a href="{{ path('app_logout') }}" class="block py-2 px-4 text-lg text-red-900 hover:text-white hover:bg-blue-700 rounded">Déconnexion</a>
                        </li>
                        {% else %}
                        <li>
                            <a href="{{ path('app_register') }}" class="block py-2 px-4 text-lg text-red-900 hover:text-white hover:bg-blue-700 rounded">Inscription</a>
                        </li>
                        <li>
                            <a href="{{ path('app_login') }}" class="block py-2 px-4 text-lg text-red-900 hover:text-white hover:bg-blue-700 rounded">Connexion</a>
                        </li>
                        {% endif %}
                    </ul>
                </div>
            </div>
        </nav>
    </header>
    <main class="min-h-screen w-[90%] lg:w-[80%] mx-auto py-6">
        {% block body %}{% endblock %}
    </main>
    <footer class="bg-white dark:bg-gray-800 py-4">
        <div class="max-w-screen-xl mx-auto p-4 flex flex-col md:flex-row justify-between items-center space-y-3 md:space-y-0">
            <span class="text-sm text-gray-500 dark:text-gray-400 text-center">© 2023 MYPULL. Tous droits réservés.</span>
            <ul class="flex space-x-4 text-sm text-gray-500 dark:text-gray-400">
                <li><a href="#" class="hover:underline">À propos</a></li>
                <li><a href="#" class="hover:underline">Politique de confidentialité</a></li>
                <li><a href="#" class="hover:underline">Contact</a></li>
            </ul>
        </div>
    </footer>
</body>
</html>
